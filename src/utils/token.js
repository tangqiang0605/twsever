// const jwt = require('jsonwebtoken');

// let str = "{username:zhangsan,pwd:12345}";

// const secret = 'twserver-token-key';

// const token = jwt.sign(str, secret);
// // const token = jwt.sign(str, secret,{algorithm:'ES256',expiresIn:1000});

// console.log(token);

// console.log(jwt.verify(token, secret));